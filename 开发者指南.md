# LiteNote 开发者指南

> 本文档描述的是 **当前仓库已落地的开发方式**。  
> 如果你在其他文档里看到与当前代码不一致的旧技术栈、旧目录或未来方案，请将其视为历史信息或规划内容，而不是当前实现。

## 一、快速开始

### 1.1 环境要求

```text
Node.js >= 18.18.0
pnpm >= 9.0.0
操作系统：Windows / macOS / Linux
```

### 1.2 安装依赖

```bash
pnpm install
```

### 1.3 启动方式

```bash
# 同时启动前端和后端
pnpm dev

# 仅启动前端
pnpm dev:web

# 仅启动后端
pnpm dev:api
```

开发地址：

- Web：`http://localhost:3000`
- API：`http://localhost:8787`

### 1.4 当前可用校验命令

```bash
pnpm build
pnpm lint
```

> 当前仓库只保证 `pnpm dev`、`pnpm dev:web`、`pnpm dev:api`、`pnpm build`、`pnpm lint` 这些脚本存在；不要把旧文档中的其他命令当作现状。

---

## 二、仓库结构

### 2.1 Monorepo 结构

```text
litenote/
├─ apps/
│  ├─ web/                    # React + Vite 前端
│  └─ api/                    # Express + TypeScript 后端
├─ memory/                    # 过程记录、截图、临时备忘
├─ reference/                 # 外部参考原型
├─ package.json               # 根脚本入口
├─ pnpm-workspace.yaml        # workspace 定义（当前仅 apps/*）
├─ README.md
├─ 开发者指南.md
├─ API设计文档.md
├─ UI设计规范文档.md
├─ UI页面线框图设计.md
└─ 项目规划文档.md
```

### 2.2 `apps/web` 结构

```text
apps/web/
├─ src/
│  ├─ components/             # 共享 UI 组件
│  ├─ views/                  # Discover / Journal / Library / Workshop / Settings
│  ├─ api.ts                  # 前端 API 请求封装
│  ├─ App.tsx                 # 前端主入口
│  ├─ i18n.ts                 # 中英文本地文案
│  ├─ main.tsx                # React 挂载入口
│  ├─ styles.css              # 全局样式与设计 token
│  └─ types.ts                # 前端类型定义
├─ package.json
└─ vite.config.ts
```

### 2.3 `apps/api` 结构

```text
apps/api/
├─ src/
│  ├─ data.ts                 # 初始种子数据
│  ├─ index.ts                # Express 服务入口
│  ├─ store.ts                # 数据读写与 dashboard 组装
│  └─ types.ts                # API / 数据模型类型
├─ data/
│  └─ litenote-db.json        # 本地 JSON 数据库
└─ package.json
```

---

## 三、前端实现说明

### 3.1 技术选型

- React 19
- Vite 6
- TypeScript
- Tailwind CSS v4（通过 `@tailwindcss/vite` 接入）
- Lucide React

### 3.2 页面组织方式

前端不是路由驱动的多页面应用，而是单页应用中的 **多视图切换**：

- `App.tsx` 维护当前视图、dashboard 数据、笔记选择、草稿状态与保存流程
- `components/AppShell.tsx` 提供侧边栏、顶部栏、搜索与全局创建按钮
- `views/DiscoverView.tsx`、`JournalView.tsx`、`LibraryView.tsx`、`WorkshopView.tsx`、`SettingsView.tsx` 负责页面级展示

### 3.3 样式系统

当前项目样式采用 **Tailwind CSS v4 + 全局 CSS 变量** 的混合方案：

- `vite.config.ts` 中启用了 `@tailwindcss/vite`
- `src/styles.css` 中通过 `@theme`、`:root`、`@utility` 定义视觉 token、辅助类与全局样式
- 组件内部主要使用 Tailwind 类名完成布局与状态表达

这意味着：

- 视觉规范仍然可以继续参考 `UI设计规范文档.md`
- 但具体落地时应同时查看 `styles.css` 与对应组件 JSX，而不是寻找旧式的局部样式写法

### 3.4 前端 API 调用

`apps/web/src/api.ts` 是当前唯一 API 入口：

- 默认基址：`/api`
- 可通过 `VITE_API_BASE_URL` 覆盖
- 使用浏览器原生 `fetch`
- 当前封装的方法包括：
  - `fetchDashboard`
  - `createNote`
  - `updateNote`
  - `toggleFavorite`
  - `archiveNote`

在本地开发环境中，Vite 会把 `/api` 代理到 `http://localhost:8787`。

---

## 四、后端实现说明

### 4.1 服务形态

当前后端是一个轻量 Express 服务，不是桌面端桥接层，也不是云端 REST 平台：

- 入口：`apps/api/src/index.ts`
- 默认端口：`8787`
- 中间件：`cors` + `express.json()`

### 4.2 当前已实现接口

- `GET /api/health`
- `GET /api/dashboard`
- `POST /api/notes`
- `PATCH /api/notes/:id`
- `POST /api/notes/:id/favorite`
- `POST /api/notes/:id/archive`

### 4.3 数据持久化

当前开发态数据保存在：

```text
apps/api/data/litenote-db.json
```

行为特点：

- 文件不存在时会自动生成种子数据
- `store.ts` 负责读写文件与组装 dashboard 响应
- 笔记摘要由服务端根据正文自动生成
- 归档逻辑本质上是把 `isArchived` 置为 `true`

> 该 JSON 文件已被 `.gitignore` 忽略，通常不应作为提交目标。

---

## 五、推荐开发流程

### 5.1 做 UI 改动时

建议同时查看这些位置：

- `apps/web/src/App.tsx`
- `apps/web/src/components/AppShell.tsx`
- 对应 `views/*.tsx`
- `apps/web/src/styles.css`
- `UI设计规范文档.md`
- `UI页面线框图设计.md`

### 5.2 做 API 改动时

建议同时查看：

- `apps/api/src/index.ts`
- `apps/api/src/store.ts`
- `apps/api/src/types.ts`
- `API设计文档.md`

### 5.3 修改前的判断原则

- **当前实现问题**：优先以代码和 `package.json` 为准
- **长期方向问题**：再参考 `项目规划文档.md`、`编辑器选型报告.md`
- 如果文档和代码冲突，请优先修正文档，不要反向假设代码“漏实现了旧文档内容”

---

## 六、构建与验证

### 6.1 本地构建

```bash
pnpm build
```

这会递归执行：

- `apps/web` 的 TypeScript 检查与 Vite 构建
- `apps/api` 的 TypeScript 编译

### 6.2 代码检查

```bash
pnpm lint
```

当前这里的 `lint` 更接近 **TypeScript 无输出检查**，不是 ESLint + Prettier 全套链路。

### 6.3 手动验收建议

- 前端页面能在 `http://localhost:3000` 正常打开
- `Discover / Journal / Library / Workshop / Settings` 五个视图可切换
- 新建、保存、收藏、归档笔记流程可正常调用 API
- 刷新后数据能从本地 JSON 数据库恢复

---

## 七、常见问题

### 7.1 前端无法访问 API

先确认：

- `apps/api` 已启动
- API 运行在 `8787`
- `apps/web/vite.config.ts` 中 `/api` 代理未被改坏
- 是否意外设置了错误的 `VITE_API_BASE_URL`

### 7.2 端口冲突

当前默认端口：

- Web：`3000`
- API：`8787`

Windows 可用：

```bash
netstat -ano | findstr :3000
netstat -ano | findstr :8787
```

### 7.3 文档看起来对不上代码

这是已知情况。请优先查看：

1. 当前源码
2. 根目录 `package.json`
3. `README.md`
4. 本文档

规划类文档中的旧技术栈、桌面端架构、云端 API 与组件系统内容，默认都不是当前实现。

---

## 八、参考资料

- [React 文档](https://react.dev)
- [Vite 文档](https://vitejs.dev)
- [TypeScript 文档](https://www.typescriptlang.org)
- [Express 文档](https://expressjs.com)
- [Tailwind CSS 文档](https://tailwindcss.com)
- [UI设计规范文档](./UI设计规范文档.md)
- [API设计文档](./API设计文档.md)
- [项目规划文档](./项目规划文档.md)

---

**文档版本**: 3.0  
**最后更新**: 2026 年 4 月 1 日
