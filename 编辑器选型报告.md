# LiteNote Markdown 编辑器选型报告

> **阶段说明**  
> 本文档用于讨论未来编辑器增强方案，不代表当前仓库已经接入富文本编辑器。  
> 当前 `Journal` 视图仍使用 React 表单控件与现有 API 完成编辑流程。

## 选型结论

> **未来优先候选：Tiptap v3**
>
> 原因不是“它适合 Vue”，而是它基于 ProseMirror、扩展能力强、UI 无头、便于在 LiteNote 的手账视觉体系中做深度定制。

---

## 候选方案对比

| 维度 | Tiptap v3 | Milkdown v7 | md-editor-v3 |
|------|----------|-------------|--------------|
| 底层引擎 | ProseMirror | ProseMirror | 自研 |
| React 集成 | ⭐⭐⭐⭐⭐ 成熟 | ⭐⭐⭐ 良好 | ⭐⭐⭐ 一般 |
| 架构 | 无头（UI 完全自定义） | 无头（插件驱动） | 开箱即用 |
| Markdown 支持 | 双向转换（导入/导出） | 原生 Markdown | 原生 Markdown |
| 扩展机制 | 官方/社区扩展丰富 | 插件系统，生态较小 | 有限 |
| 协作编辑 | Yjs 生态成熟 | 社区方案 | 不支持 |
| 自定义 UI | 完全自由 | 完全自由 | 受限于预设 UI |
| 社区活跃度 | ⭐⭐⭐⭐⭐ 非常活跃 | ⭐⭐⭐ 中等 | ⭐⭐⭐ 中等 |
| 学习曲线 | 中等 | 中等 | 低 |
| 包体积 (gzip) | ~45KB | ~40KB | ~80KB |

---

## 选择 Tiptap 的理由

### 1. 适合未来的组件化内容模型

Tiptap 的 Node / Mark 扩展机制适合承载 LiteNote 后续的内容块能力：

```text
LiteNote 内容能力      →  Tiptap 扩展方式
────────────────────────────────────
标题块                →  Heading Extension
待办事项              →  TaskList Extension
代码块                →  CodeBlock Extension
图片块                →  Image Extension
表格块                →  Table Extension
自定义装饰块          →  自定义 Node Extension
```

### 2. 更适合手账风 UI

LiteNote 对视觉表达要求很高。无头编辑器方案可以让我们继续使用现有的 Tailwind CSS v4、全局 token 与自定义排版体系，而不是被预制编辑器 UI 限制。

### 3. 有利于后续扩展

- 协作编辑可以接入 Yjs
- 组件市场可以映射为扩展分发
- AI 辅助写作、样式增强等能力也更容易插入

---

## 当前状态与接入建议

当前仓库尚未引入 Tiptap 相关依赖。若未来进入编辑器升级阶段，建议采用 React 版本集成，而不是旧方案里的 Vue 绑定。

建议依赖方向：

```json
{
  "dependencies": {
    "@tiptap/react": "^3.x",
    "@tiptap/pm": "^3.x",
    "@tiptap/starter-kit": "^3.x",
    "@tiptap/extension-placeholder": "^3.x",
    "@tiptap/extension-typography": "^3.x",
    "@tiptap/extension-highlight": "^3.x",
    "@tiptap/extension-task-list": "^3.x",
    "@tiptap/extension-task-item": "^3.x",
    "@tiptap/extension-image": "^3.x"
  }
}
```

---

## 未来集成方向

建议未来在 `apps/web/src/components/editor/` 下建立独立编辑器子系统：

```text
apps/web/src/components/editor/
├─ Editor.tsx
├─ EditorToolbar.tsx
├─ EditorBubbleMenu.tsx
├─ EditorContent.tsx
└─ extensions/
   ├─ JournalBlock.ts
   └─ HandwritingFont.ts
```

核心思路：

- `JournalView` 继续作为页面容器
- 编辑器能力逐步替换当前 `textarea`
- 样式依然复用现有设计 token，而不是引入一套独立视觉系统

---

## 结论

- 当前实现：仍是基础文本编辑流
- 未来候选：优先考虑 Tiptap React 集成
- 保留原则：先满足 Web MVP 的稳定体验，再推进富文本与组件化编辑

---

**文档版本**: 2.0
**最后更新**: 2026 年 4 月 1 日
