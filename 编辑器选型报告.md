# LiteNote Markdown 编辑器选型报告

## 选型结论

> **选定方案：Tiptap v3**
>
> 基于 ProseMirror 的无头富文本框架，Vue 3 原生支持，扩展机制与 Phase 2 组件系统天然契合。

---

## 候选方案对比

| 维度 | Tiptap v3 | Milkdown v7 | md-editor-v3 |
|------|----------|-------------|--------------|
| 底层引擎 | ProseMirror | ProseMirror | 自研 |
| Vue 3 支持 | ⭐⭐⭐⭐⭐ 一等公民 | ⭐⭐⭐⭐ 良好 | ⭐⭐⭐⭐ 良好 |
| 架构 | 无头（UI 完全自定义） | 无头（插件驱动） | 开箱即用 |
| Markdown 支持 | 双向转换（导入/导出） | 原生 Markdown | 原生 Markdown |
| 扩展机制 | 100+ 官方/社区扩展 | 插件系统，生态较小 | 有限 |
| 协作编辑 | Yjs + Hocuspocus | 社区方案 | 不支持 |
| 自定义 UI | 完全自由 | 完全自由 | 受限于预设 UI |
| 社区活跃度 | ⭐⭐⭐⭐⭐ 非常活跃 | ⭐⭐⭐ 中等 | ⭐⭐⭐ 中等 |
| 学习曲线 | 中等 | 中等 | 低 |
| 包体积 (gzip) | ~45KB (starter-kit) | ~40KB | ~80KB |

---

## 选择 Tiptap 的理由

### 1. Phase 2 组件系统的天然基础
Tiptap 的 Node/Mark 扩展机制与 LiteNote 的「内容组件块」概念完美对齐：

```
LiteNote 组件         →  Tiptap 实现
─────────────────────────────────────
标题块               →  Heading Extension
待办事项             →  TaskList Extension
代码块               →  CodeBlockLowlight Extension
图片块               →  Image Extension
表格块               →  Table Extension (TableKit)
自定义装饰块(Phase 2) →  自定义 Node Extension
```

### 2. 手账风格 UI 完全可控
无头架构意味着编辑器不施加任何 CSS，我们可以 100% 按照 UI 设计规范实现手账风格，不需要覆盖任何框架默认样式。

### 3. 面向未来
- Phase 3 的协作编辑 → Tiptap 内置 Yjs 支持
- AI 辅助功能 → Tiptap Content AI 扩展
- 组件市场 → 社区组件可实现为 Tiptap Extension 分发

---

## Phase 1a 所需依赖

```json
{
  "dependencies": {
    "@tiptap/vue-3": "^3.x",
    "@tiptap/pm": "^3.x",
    "@tiptap/starter-kit": "^3.x",
    "@tiptap/extension-placeholder": "^3.x",
    "@tiptap/extension-typography": "^3.x",
    "@tiptap/extension-highlight": "^3.x",
    "@tiptap/extension-task-list": "^3.x",
    "@tiptap/extension-task-item": "^3.x",
    "@tiptap/extension-image": "^3.x"
  }
}
```

> `starter-kit` 包含：Document, Paragraph, Text, Bold, Italic, Strike, Code, Heading, BulletList, OrderedList, ListItem, Blockquote, HorizontalRule, HardBreak, History (Undo/Redo)

---

## 集成架构

```
apps/web/src/components/editor/
├─ Editor.vue              # 主编辑器组件（useEditor）
├─ EditorToolbar.vue       # 工具栏（按钮调用 editor.chain()）
├─ EditorBubbleMenu.vue    # 选中文本浮动菜单
├─ EditorContent.vue       # 内容区域（Tiptap EditorContent）
└─ extensions/             # 自定义扩展
   ├─ HandwritingFont.ts   # 手写字体切换扩展（Phase 1b）
   └─ JournalBlock.ts      # 手账装饰块扩展（Phase 2）
```

```typescript
// 核心集成示例
import { useEditor, EditorContent } from '@tiptap/vue-3'
import StarterKit from '@tiptap/starter-kit'

const editor = useEditor({
  extensions: [StarterKit],
  content: note.value.content,
  onUpdate: ({ editor }) => {
    // 自动保存 Markdown
    note.value.content = editor.storage.markdown.getMarkdown()
  }
})
```

---

**文档版本**: 1.0
**最后更新**: 2026 年 3 月 30 日
