# LiteNote - 项目规划文档

> **文档定位**  
> 本文档主要服务于中长期路线图与未来架构规划，不代表当前仓库的已实现状态。  
> 当前真实技术栈、目录和运行方式请优先查看 `README.md`、`开发者指南.md`、`apps/web` 与 `apps/api`。

## 一、项目定位

### 1.1 产品愿景
**LiteNote** 是一个轻量级、手账风、开源的跨平台记事本应用，致力于为用户提供美观、易用、高度个性化的笔记体验。强调：
- 🎨 手账美学设计
- 🧩 模块化组件系统
- 🌈 丰富的主题风格
- 🤝 社区驱动的组件生态

### 1.2 核心目标
1. **打造最美的开源笔记应用** - 超越同类开源产品的 UI/UX
2. **建立组件生态** - 让用户和开发者参与设计
3. **轻量高效** - 启动快、体积小、资源占用低
4. **完全开源** - 社区驱动，鼓励贡献

### 1.3 目标用户
- 学生：快速记笔记、整理知识
- 手账爱好者：喜欢美观设计、丰富组件
- 创意工作者：灵感记录、内容创作
- 开发者：感兴趣开源、想参与贡献

---

## 二、产品定位：手账风记事本

### 2.1 什么是"手账风"

手账风的核心特征：
```
视觉层面：
├─ 纸张质感 - 柔和色调、纹理背景
├─ 手写感 - 字体风格、笔画粗细变化
├─ 精细排版 - 装饰线、分隔符、排版讲究
└─ 色彩搭配 - 清新、温暖、舒适的配色

交互层面：
├─ 页面概念 - 像翻页一样的交互
├─ 标记系统 - 便签条、标签、书签
├─ 组织方式 - 日期为主、分类为辅
└─ 细节动画 - 轻微、舒适的过渡效果
```

### 2.2 手账风 vs 其他风格对比

| 特性 | 手账风 | 极简风 | 专业风 | 卡片风 |
|------|------|------|------|-------|
| 色彩 | 柔和、丰富 | 黑白灰 | 蓝色系 | 彩色 |
| 装饰 | 多（线条、图案） | 无 | 少 | 中等 |
| 字体 | 显示体+衬线 | 无衬线 | 无衬线 | 无衬线 |
| 质感 | 纸张、麻布 | 扁平 | 玻璃态 | 投影 |
| 交互 | 翻页、标记 | 快速切换 | 列表式 | 滚动 |
| 目标用户 | 创意、学生 | 开发者 | 企业 | 大众 |

---

## 三、核心功能架构

### 3.1 功能框架

```
LiteNote
│
├─ 笔记管理
│  ├─ 新建笔记
│  ├─ 编辑笔记
│  ├─ 删除笔记
│  └─ 搜索笔记
│
├─ 组织系统
│  ├─ 日期组织（日记模式）
│  ├─ 文件夹分类
│  ├─ 标签系统
│  └─ 快速访问（收藏夹）
│
├─ 编辑功能
│  ├─ Markdown 编辑
│  ├─ 富文本编辑
│  ├─ 手写输入（可选）
│  └─ 多媒体嵌入
│
├─ 组件系统
│  ├─ 内置组件库
│  ├─ 用户组件库
│  ├─ 社区组件市场
│  └─ 组件编辑器
│
├─ 风格系统
│  ├─ 主题管理
│  ├─ 色彩方案
│  ├─ 字体选择
│  └─ 布局预设
│
└─ 数据管理
   ├─ 本地存储
   ├─ 导出/导入
   ├─ 备份恢复
   └─ 云同步（可选）
```

### 3.2 功能优先级

#### Phase 1a（Web MVP）- 当前已完成基线
```
当前已落地：
- Web 前端 + 本地 Express API 双应用结构
- 新建/编辑/归档/收藏笔记
- Discover / Journal / Library / Workshop / Settings 五个主视图
- JSON 文件持久化（当前开发态）
- 基础手账风格 UI（参见 UI 设计规范「MVP 最小美学标准」）

当前状态：Phase 1a 已形成可运行的 Web MVP 基线
```

#### Phase 1b（桌面探索）- 打磨与打包
```
在当前 Web MVP 基础上探索：
- Tauri 桌面应用打包
- SQLite 本地数据库（替换当前开发态 JSON 持久化）
- 手账风格 UI 精细打磨
- 导出功能（Markdown / HTML）
- 文件夹分类

交付目标：验证桌面端可行性并收敛稳定持久化方案
```

#### Phase 2（增强）- 组件系统
```
优先实现：
- 内置组件库（10-15 个常用组件）
- 组件拖放编辑
- 基础主题系统（3 个官方主题）
- 标签分类

交付目标：支持个性化编辑的笔记应用
预计周期：2-3 个月
```

#### Phase 3（生态）- 开源生态 + 可选云端
```
社区建设：
- 组件市场框架
- 组件开发工具和 CLI
- 可选云同步（参见 API 设计文档）
- 社区指南和文档完善

交付目标：可扩展的社区驱动平台
预计周期：持续迭代
```

---

## 四、组件系统设计

### 4.1 组件分类

#### 内容组件（Content Blocks）
用户在笔记中可添加的各种内容形式：

```
文字类：
├─ 标题块（大/中/小）
├─ 正文段落
├─ 代码块
├─ 引用块
└─ 列表块

媒体类：
├─ 图片块
├─ 音频块
├─ 视频块
└─ 附件块

结构化：
├─ 表格块
├─ 待办事项
├─ 时间线
└─ 分隔线

装饰类：
├─ 背景颜色块
├─ 贴纸/装饰框
├─ 标签贴条
└─ 手写签名
```

#### UI 组件（UI Components）
应用本身的 UI 组件，可主题化：

```
导航类：
├─ 侧边栏
├─ 标签页
├─ 面包屑
└─ 下拉菜单

表单类：
├─ 输入框
├─ 日期选择器
├─ 颜色选择器
└─ 搜索框

反馈类：
├─ 模态框
├─ 通知提示
├─ 加载动画
└─ 确认对话框
```

### 4.2 组件结构设计

每个组件包含：

```json
{
  "component": {
    "id": "component_id",
    "name": "组件名称",
    "version": "1.0.0",
    "category": "content", // 或 ui
    "author": "作者",
    "description": "组件描述",
    
    "schema": {
      // 组件配置项
      "properties": {
        "text": { "type": "string" },
        "color": { "type": "string" },
        "size": { "type": "number" }
      }
    },
    
    "ui": {
      "template": "HTML 模板",
      "styles": "CSS 样式"
    },
    
    "preview": "预览图",
    "tags": ["标签1", "标签2"],
    "compatibility": "v1.0+"
  }
}
```

### 4.3 内置组件库示例（第二阶段）

```
标题组件
├─ H1 标题（大标题）
├─ H2 标题（中标题）
├─ H3 标题（小标题）
└─ 装饰标题（带图标/颜色）

列表组件
├─ 无序列表
├─ 有序列表
├─ 待办列表（可勾选）
└─ 嵌套列表

突出组件
├─ 黄色标记
├─ 彩色标记
├─ 引用区块
└─ 提示框（Info/Warning/Error）

时间组件
├─ 日期标签
├─ 时间线
├─ 计时器
└─ 倒计时

装饰组件
├─ 分隔线
├─ 空白间距
├─ 装饰框
└─ 角标贴纸
```

### 4.4 组件开发流程

用户开发自定义组件的流程：

```
1. 选择模板
   └─ 内容组件模板 / UI 组件模板

2. 编辑组件
   ├─ 编写 HTML/CSS/JS
   ├─ 定义参数 Schema
   └─ 上传预览图

3. 预览测试
   └─ 在应用中实时预览

4. 发布到社区
   ├─ 填写组件元数据
   ├─ 设置版本和兼容性
   └─ 提交到组件市场

5. 版本管理
   ├─ 更新维护
   ├─ 获得反馈
   └─ 持续优化
```

---

## 五、风格/主题系统

### 5.1 主题架构

每个主题包含：

```
主题结构
├─ 色彩方案
│  ├─ 主色调
│  ├─ 辅助色
│  ├─ 背景色
│  ├─ 文字色
│  └─ 中性灰
├─ 字体方案
│  ├─ 标题字体
│  ├─ 正文字体
│  ├─ 代码字体
│  └─ 装饰字体
├─ 间距/大小
│  ├─ 圆角度数
│  ├─ 阴影深度
│  ├─ 边框宽度
│  └─ 内间距预设
└─ 动画/过渡
   ├─ 过渡时间
   ├─ 动画速度
   └─ 简历曲线
```

### 5.2 内置主题方案（第一批）

#### 1. **清晨**（Light - 默认）
- 白色背景、柔和蓝绿色主题
- 纸张质感、清爽舒适
- 适合日间使用

#### 2. **夜影**（Dark）
- 深灰背景、柔和紫色主题
- 护眼舒适、现代感
- 适合夜间使用

#### 3. **樱花**（Pink）
- 浅粉背景、粉色系主题
- 甜美柔和、少女感
- 适合学生用户

#### 4. **森林**（Green）
- 淡绿背景、绿色系主题
- 自然舒适、宁静感
- 适合长时间工作

#### 5. **日落**（Warm）
- 米黄背景、橙红色主题
- 温暖怀旧、复古感
- 适合回顾反思

#### 6. **极简**（Minimal）
- 纯白背景、黑色文字
- 无装饰、高效简洁
- 适合快速记录

### 5.3 主题定制

用户可以：
```
预设主题
  └─ 快速应用（一键切换）

主题编辑
  ├─ 调整色彩
  ├─ 更换字体
  ├─ 修改间距
  └─ 预览效果

主题导出/分享
  ├─ 导出为 JSON
  ├─ 分享到社区
  └─ 导入他人主题
```

---

## 六、社区与生态

### 6.1 开源社区建设

#### 组件市场（Component Store）
```
功能：
- 浏览和搜索组件
- 一键安装使用
- 评分和反馈
- 版本管理

结构：
LiteNote-Components/
├─ Official/（官方组件）
├─ Community/（社区组件）
├─ Themes/（社区主题）
└─ Tools/（工具脚本）
```

#### 开发者资源
```
文档：
- 组件开发指南
- API 参考文档
- 最佳实践
- 示例项目

工具：
- 组件脚手架（CLI）
- 组件编辑器
- 本地测试框架
- 发布工具

社区：
- GitHub Discussions
- Discord 服务器
- 中文论坛
- 周报和博客
```

### 6.2 贡献方式

用户可以通过以下方式参与：

```
贡献类型        难度    奖励
────────────────────────────
报告 Bug         ⭐      积分
完善文档         ⭐⭐     积分
设计主题         ⭐⭐     徽章
开发组件         ⭐⭐⭐   徽章+推荐
翻译本地化       ⭐⭐     积分
提议功能         ⭐      反馈
代码贡献         ⭐⭐⭐⭐  Contributor
维护组件         ⭐⭐⭐   Official Badge
```

### 6.3 组件上架流程

```
开发者提交
    ↓
自动检查
  ├─ 代码质量检查
  ├─ 安全扫描
  └─ 兼容性验证
    ↓
社区评审
  ├─ 代码审查
  ├─ 功能验证
  └─ 用户反馈
    ↓
上架发布
  ├─ 组件标记
  ├─ 版本记录
  └─ 社区推荐
    ↓
持续维护
  ├─ 更新管理
  ├─ Bug 修复
  └─ 用户支持
```

---

## 七、技术架构建议

### 7.1 技术栈

> 本节包含“当前已实现”与“未来候选方案”两部分，请不要把后者误认为当前代码现状。

```
当前已实现【Phase 1a】：
├─ React 19 + TypeScript
├─ Vite（构建工具）
├─ Tailwind CSS v4 + CSS 变量 + 全局样式文件
├─ Express 本地 API 服务
└─ JSON 文件持久化（开发态）

桌面应用【Phase 1b 候选】：
├─ Tauri（跨平台，轻量）
└─ SQLite（本地数据库）

移动应用【远期探索】：
├─ 待定（Flutter / Capacitor / 响应式 Web 适配）
└─ 视桌面版成熟度和社区需求决定方案

云端服务【Phase 3+ 候选】：
├─ 可选云同步（参见 API 设计文档）
├─ 组件市场后端
└─ 用户认证体系

编辑器 / 组件系统【Phase 2 候选】：
├─ 候选富文本编辑器：Tiptap v3
├─ JSON Schema 定义
└─ 独立内容块与主题扩展能力
```

### 7.2 项目结构

> 下列结构分为“当前仓库实际存在”与“未来可能扩展”两类。

```
litenote/
├─ apps/
│  ├─ web/              # 当前已实现：React Web 应用
│  └─ api/              # 当前已实现：Express API
├─ memory/              # 当前存在：过程记录与截图
├─ reference/           # 当前存在：参考原型
├─ 根目录文档           # 当前存在：规划、设计、API、开发说明
│
├─ desktop/             # 未来候选：Tauri 桌面端
├─ mobile/              # 未来候选：移动端
├─ packages/            # 未来候选：共享能力拆分
├─ community/           # 未来候选：社区资源
└─ tools/               # 未来候选：开发工具
```

---

## 八、设计系统规范

### 8.1 色彩系统

```
主色系（可根据主题变化）
├─ 主色：#5B9BD5
├─ 辅助色 1：#FFB74D
├─ 辅助色 2：#81C784
└─ 辅助色 3：#E57373

中性色（保持一致）
├─ 白色：#FFFFFF
├─ 浅灰：#F5F5F5
├─ 中灰：#AEAEAE
├─ 深灰：#424242
└─ 黑色：#212121

语义色（保持一致）
├─ 成功：#4CAF50
├─ 警告：#FF9800
├─ 错误：#F44336
└─ 信息：#2196F3
```

### 8.2 字体系统

```
英文字体：
├─ 标题：Inter / Poppins
├─ 正文：Inter / Open Sans
└─ 代码：Fira Code / JetBrains Mono

中文字体：
├─ 标题：思源黑体 / HarmonyOS Sans
├─ 正文：思源宋体 / PingFang
└─ 代码：JetBrains Mono / Consolas

字体大小：
├─ H1: 32px (粗)
├─ H2: 24px (粗)
├─ H3: 20px (中)
├─ 正文: 14px (常规)
└─ 小字: 12px (常规)
```

### 8.3 间距系统

```
基础单位：4px

预设规格：
├─ xs: 4px
├─ sm: 8px
├─ md: 16px
├─ lg: 24px
├─ xl: 32px
└─ 2xl: 48px

应用规则：
├─ 内间距（padding）：md / lg / xl
├─ 外间距（margin）：sm / md / lg
├─ 圆角（border-radius）：4px / 8px / 16px
└─ 阴影（box-shadow）：浅/中/深
```

---

## 九、数据模型设计

### 9.1 核心数据模型

```typescript
// 笔记
interface Note {
  id: string             // UUID
  title: string          // 标题
  content: string        // Markdown 内容
  blocks: Block[]        // 组件化内容块（Phase 2）
  category_id?: string   // 分类 ID
  tags: string[]         // 标签 ID 列表
  is_favorite: boolean   // 收藏
  is_deleted: boolean    // 软删除
  created_at: string     // ISO 8601
  updated_at: string     // ISO 8601
  version: number        // 版本号（乐观锁）
}

// 内容块（Phase 2 组件系统使用）
interface Block {
  id: string
  type: string           // 组件类型标识
  properties: Record<string, any>  // 组件配置
  order: number          // 排序序号
}

// 分类
interface Category {
  id: string
  name: string
  color: string          // HEX 颜色
  icon: string           // 图标标识
  description?: string
  parent_id?: string     // 嵌套分类（可选）
  order: number
  created_at: string
}

// 标签
interface Tag {
  id: string
  name: string
  color?: string
  created_at: string
}

// 主题（Phase 2）
interface Theme {
  id: string
  name: string
  type: 'builtin' | 'custom'
  colors: Record<string, string>
  fonts: Record<string, string>
  spacing: Record<string, string>
  created_at: string
}

// 用户设置（本地）
interface UserSettings {
  language: string       // 默认 'zh-CN'
  theme_id: string       // 当前主题
  auto_save: boolean     // 自动保存
  auto_save_interval: number  // 秒
  editor_font_size: number
  sidebar_collapsed: boolean
}
```

### 9.2 数据关系

```
Note ──┬── Category (多对一)
       ├── Tag (多对多，通过 note_tags 关联)
       └── Block[] (一对多，内嵌)

Theme ── UserSettings.theme_id (一对一引用)
```

---

## 十、本地存储架构

### 10.1 存储策略

```
当前 Web MVP（已实现）：
├─ Express 本地 API
├─ JSON 文件持久化（开发态）
│  └─ apps/api/data/litenote-db.json
└─ LocalStorage（轻量配置）
   └─ 语言与少量本地 UI 偏好

早期候选思路（未采用）：
├─ IndexedDB
└─ 更纯前端的本地存储层

说明：
├─ 当前仓库已采用“前端 + 本地 API + JSON 文件”组合
└─ IndexedDB 不再是当前主路线，但可作为离线能力探索参考

Phase 1b（桌面版）：
├─ SQLite（主数据库）
│  └─ 见下方表结构
├─ 文件系统（附件存储）
│  └─ {app_data}/attachments/{note_id}/
└─ JSON 文件（配置）
   └─ {app_data}/settings.json
```

### 10.2 SQLite 表结构（Phase 1b）

```sql
-- 笔记表
CREATE TABLE notes (
  id          TEXT PRIMARY KEY,
  title       TEXT NOT NULL DEFAULT '',
  content     TEXT NOT NULL DEFAULT '',
  category_id TEXT REFERENCES categories(id) ON DELETE SET NULL,
  is_favorite INTEGER NOT NULL DEFAULT 0,
  is_deleted  INTEGER NOT NULL DEFAULT 0,
  version     INTEGER NOT NULL DEFAULT 1,
  created_at  TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at  TEXT NOT NULL DEFAULT (datetime('now'))
);

-- 分类表
CREATE TABLE categories (
  id          TEXT PRIMARY KEY,
  name        TEXT NOT NULL,
  color       TEXT NOT NULL DEFAULT '#5B9BD5',
  icon        TEXT NOT NULL DEFAULT 'folder',
  description TEXT DEFAULT '',
  parent_id   TEXT REFERENCES categories(id) ON DELETE SET NULL,
  sort_order  INTEGER NOT NULL DEFAULT 0,
  created_at  TEXT NOT NULL DEFAULT (datetime('now'))
);

-- 标签表
CREATE TABLE tags (
  id          TEXT PRIMARY KEY,
  name        TEXT NOT NULL UNIQUE,
  color       TEXT DEFAULT NULL,
  created_at  TEXT NOT NULL DEFAULT (datetime('now'))
);

-- 笔记-标签关联表
CREATE TABLE note_tags (
  note_id TEXT NOT NULL REFERENCES notes(id) ON DELETE CASCADE,
  tag_id  TEXT NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
  PRIMARY KEY (note_id, tag_id)
);

-- 索引
CREATE INDEX idx_notes_category ON notes(category_id);
CREATE INDEX idx_notes_created ON notes(created_at);
CREATE INDEX idx_notes_updated ON notes(updated_at);
CREATE INDEX idx_notes_favorite ON notes(is_favorite);
CREATE INDEX idx_note_tags_tag ON note_tags(tag_id);
```

### 10.3 文件系统布局（桌面版）

```
{app_data}/
├─ litenote.db          # SQLite 主数据库
├─ settings.json        # 用户设置
├─ themes/              # 自定义主题
│  └─ {theme_id}.json
├─ attachments/         # 附件目录
│  └─ {note_id}/
│     ├─ image_001.png
│     └─ file_002.pdf
├─ backups/             # 自动备份
│  └─ backup_2026-03-30.db
└─ exports/             # 导出临时目录
```

---

## 十一、开发路线图

### 第一阶段（已完成）- Web MVP 基线
- [x] 项目初始化，技术栈搭建
- [x] Web 前端与本地 API 联通
- [x] 核心笔记创建、更新、收藏、归档流程
- [x] Discover / Journal / Library / Workshop / Settings 主视图
- [x] 基础手账风格 UI 完成
- [x] Web MVP 版本成形

说明：
- 当前持久化方案为 Express + JSON 文件
- `IndexedDB` 属于早期候选思路，已不是当前主路线

### 第二阶段（2026 Q3-Q4）- 桌面探索与打磨
- [ ] Tauri 桌面应用打包
- [ ] SQLite 本地数据库迁移
- [ ] 手账风格 UI 精细打磨
- [ ] 导出功能实现
- [ ] 文件夹分类
- [ ] 桌面版正式发布

### 第三阶段（2027 Q1-Q2）- 组件系统
- [ ] 内置组件库开发（15 个）
- [ ] 组件拖放编辑功能
- [ ] 主题系统实现
- [ ] 3 个官方主题设计
- [ ] 组件编辑器实现
- [ ] 社区文档编写

### 第四阶段（2027 Q3+）- 生态建设 + 可选云端
- [ ] 组件市场框架开发
- [ ] 可选云同步功能
- [ ] 开发者工具 CLI
- [ ] 社区指南完善
- [ ] 推广与用户增长
- [ ] 国际化支持（视需求）

---

## 十二、营销与推广策略

### 12.1 目标定位

```
核心卖点：
- 最美的开源笔记应用
- 手账风格设计
- 高度可定制
- 活跃开源社区

目标人群：
- 设计爱好者（通过美观吸引）
- 学生用户（通过易用吸引）
- 开源爱好者（通过社区吸引）
- 创意工作者（通过组件吸引）
```

### 12.2 推广渠道

```
社区渠道：
├─ GitHub（代码仓库）
├─ Product Hunt（产品发布）
├─ v2ex/小红书（内容营销）
└─ 设计社区（Dribbble/Behance）

内容营销：
├─ 使用教程视频
├─ 主题设计分享
├─ 组件开发指南
└─ 用户案例展示

合作推广：
├─ 设计博主合作
├─ 学生社团合作
├─ 开源组织合作
└─ 开发者社区联动
```

---

## 十三、成功指标

### 13.1 关键指标（KPI）

```
用户相关（桌面版发布后 6 个月）：
├─ 用户下载量：目标 1K+
├─ 月活用户：目标 200+
└─ 用户满意度：4.0+ 星

社区相关（桌面版发布后 1 年）：
├─ GitHub Stars：目标 500+
├─ 贡献者数量：目标 20+
├─ 组件数量：目标 20+（含官方 + 社区）
└─ 社区成员：目标 100+

产品相关：
├─ Phase 1 功能完成度：100%
├─ Bug 修复率：90%+
└─ 文档完善度：80%+
```

### 13.2 进度评估

定期（每月）评估：
```
开发进度
├─ 功能完成度
├─ Bug 修复情况
├─ 性能指标
└─ 用户反馈

社区健康度
├─ Issue 响应时间
├─ Pull Request 审核时间
├─ 社区活跃度
└─ 贡献者增长
```

---

## 十四、总结

### LiteNote 核心价值主张

```
美观的设计
    ↓
吸引用户关注
    ↓
强大的组件系统
    ↓
鼓励用户创作
    ↓
活跃的社区生态
    ↓
可持续的项目发展
    ↓
最好的开源笔记应用
```

### 团队和资源

```
初期团队需求（Phase 1）：
├─ 1 位全栈开发者（主力）
├─ 1 位 UI 设计师（兼职或外包）
└─ 社区志愿贡献者

成本估算（Phase 1 周期）：
├─ 开发人力：个人项目 / 志愿驱动
├─ 设计人力：志愿 / $2K-5K（外包）
├─ 基础设施：$0-50/月（GitHub Pages / Vercel 免费层）
└─ 总计：$0-5K（开源项目模式）

增长期团队（Phase 2+）：
├─ 2-3 位全栈开发者
├─ 1 位 UI/UX 设计师
├─ 1 位社区运营
└─ 5+ 位社区贡献者
```

---

**文档版本**：3.0  
**最后更新**：2026 年 4 月 1 日  
**下一次审视**：Phase 1b 范围收敛后
